# Airflow (v1.10.9) demo

## Prerequisites

* Python

* Docker-Compose

## Installation

Create `.env` file from `.env.sample` if you would like to overwrite airflow configuration

```bash
docker-compose build

docker-compose up
```
